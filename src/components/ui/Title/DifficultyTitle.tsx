"use client";
const DifficultyTitle = ({ text }: { text: string }) => (
	<h2 className="text-cream font-pixel uppercase tracking-widest text-sm text-center mb-6">
		{text}
	</h2>
);
export default DifficultyTitle;
